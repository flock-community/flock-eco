import {storiesOf} from '@storybook/react'
import {UserGroupTable} from '../../../main/react/user_group/UserGroupTable'

storiesOf('User group/UserGroupTable', module).add('default', () => (
  <UserGroupTable />
))
